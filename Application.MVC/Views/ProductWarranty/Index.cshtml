@model IEnumerable<Application.Data.Models.ProductWarranty>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Danh sách bảo hành sản phẩm</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Tạo mới</a>
</p>

<table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Sản phẩm</th>
            <th>Thời hạn bảo hành</th>
            <th>Điều kiện bảo hành</th>
            <th>Đã tạo lúc</th>
            <th>Đã cập nhật</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Product?.Name</td>
                <td>@Html.DisplayFor(modelItem => item.WarrantyPeriod)</td>
                <td>@Html.DisplayFor(modelItem => item.WarrantyConditions)</td>
                <td>@Html.DisplayFor(modelItem => item.CreatedAt)</td>
                <td>@Html.DisplayFor(modelItem => item.UpdatedAt)</td>
                <td>
                    <!-- Update Button -->
                    @Html.ActionLink("Cập nhật", "Update", new { id = item.WarrantyID }, new { @class = "btn btn-warning btn-sm" }) |
                    <!-- Details Button -->
                    @Html.ActionLink("Chi tiết", "Details", new { id = item.WarrantyID }, new { @class = "btn btn-info btn-sm" }) |
                    <!-- Delete Button with modal trigger -->
                    <button class="btn btn-danger btn-sm" data-toggle="modal" data-target="#confirmDeleteModal" data-id="@item.WarrantyID">Xoá bỏ</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Modal -->
<div class="modal fade" id="confirmDeleteModal" tabindex="-1" role="dialog" aria-labelledby="confirmDeleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="confirmDeleteModalLabel">Confirm Deletion</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Bạn có chắc chắn muốn xóa bảo hành này không?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Huỷ bỏ</button>
                <!-- Delete button that will be triggered dynamically -->
                <a id="deleteButton" href="#" class="btn btn-danger">Xoá bỏ</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        // Set the ID of the item to delete when the modal is shown
        $('#confirmDeleteModal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget); // Button that triggered the modal
            var warrantyId = button.data('id'); // Extract the warranty ID from the data-* attributes
            var deleteUrl = '/ProductWarranty/Delete/' + warrantyId; // Construct the delete URL
            var modal = $(this);
            modal.find('#deleteButton').attr('href', deleteUrl); // Update the delete button's href with the correct URL
        });
    </script>
}